# 环境配置模板 - 复制此文件为 .env 并填入真实值

# ASR API 配置 (硅基流动) - 必填
ASR_API_KEY=sk-your-siliconflow-api-key
ASR_API_URL=https://api.siliconflow.cn/v1/audio/transcriptions
ASR_MODEL=TeleAI/TeleSpeechASR

# LLM API 配置（可选，需自备 Key；若服务不需要可留空）
LLM_API_KEY=
LLM_API_URL=https://juya.owl.ci/v1
LLM_MODEL=DeepSeek-V3.1-Terminus

# 在线音频导入限制（可选，单位字节，默认 50MB）
FETCH_AUDIO_MAX_BYTES=52428800

# WebUI 设置（可选）
CUSTOM_INSTRUCTIONS=请对以下语音转文字内容进行处理：1. 纠正错别字和语法错误 2. 添加适当的标点符号 3. 分段排版使内容更易读 4. 保持原意不变，不要添加或删除内容
PROXY_URL=http://127.0.0.1:7890

# 指定 WebUI 读写的 .env 文件路径（可选，默认: ./.env）
APP_SETTINGS_ENV_FILE=.env
